# MCP Servers

MCP server tool definitions that expose kernel functionality to LLM clients.

## Tools



## Architecture

Each tool is a thin wrapper around kernel functions, handling:


## Dependencies

Requires `kernel/` module.